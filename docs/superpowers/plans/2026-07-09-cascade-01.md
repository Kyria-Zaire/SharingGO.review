# CASCADE-01 — Cascade d'annulation & remboursement — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Faire cascader l'annulation d'un trajet sur ses réservations et ouvrir une file de traitement manuel admin (remboursement Stripe intégral ou avoir), sans aucun appel Stripe automatique.

**Architecture:** Cascade transactionnelle dans `cancelTrip()` (`prisma.$transaction`), file admin filtrée sur `refundStatus = PENDING`, actions admin refund/credit gardées par `SELECT … FOR UPDATE` + `Idempotency-Key` Stripe. Audit transactionnel via helper dédié `writeCascadeAuditLog` (le `writeAuditLog` partagé n'est pas touché). Premier socle de tests d'intégration du backend (Vitest + Postgres `.env.test`).

**Tech Stack:** Express + TypeScript (ESM strict), Prisma 6 / PostgreSQL, Stripe SDK, React + Vite (admin & passager), Vitest.

**Spec de référence :** `docs/superpowers/specs/2026-07-09-cascade-01-design.md` (toutes les décisions figées y sont).

## Global Constraints

- Branche `feature/cascade-01` — aucun push direct sur `main`, PR + review CTO avant merge.
- `ReservationStatus.CANCELED` (un L) vs `TripLifecycleStatus.CANCELLED` (deux L) — ne jamais confondre.
- **Aucun appel Stripe** déclenché automatiquement lors de l'annulation d'un trajet.
- Montant remboursé/crédité = `payment.amount` uniquement, jamais paramétrable depuis le front.
- `writeAuditLog` (`backend/src/lib/audit-log.ts`) **reste inchangé** — les 35 appelants existants ne sont pas touchés. L'audit transactionnel passe par `writeCascadeAuditLog(tx, input)`.
- Verrou concurrent via `SELECT … FOR UPDATE` — **pas** de nouvel état `PROCESSING` sur l'enum.
- Échec Stripe refund → HTTP 502 `STRIPE_REFUND_FAILED`, réservation reste `PENDING`.
- Idempotency-Key Stripe déterministe : `refund_${reservationId}`.
- Migration via `prisma migrate` — jamais de SQL raw en prod.
- TypeScript strict, `eslint --max-warnings 0`, pas de `any`, pas de secret en clair.
- Client Stripe : `getStripeClient()` (`backend/src/modules/payments/stripe.service.ts`).
- Email hors scope — ne rien improviser, ouvrir NOTIFY-01.

---

### Task 1: Socle de test d'intégration (Vitest + Postgres `.env.test`)

Aucune infra de test n'existe (script `test` = placeholder). Cette tâche pose le socle : Vitest ESM, une base Postgres de test, un helper de reset entre tests, et un premier test « sanity » qui prouve que la DB de test répond.

**Files:**
- Modify: `backend/package.json` (devDeps + scripts `test`, `test:watch`)
- Create: `backend/vitest.config.ts`
- Create: `backend/.env.test.example`
- Create: `backend/tests/helpers/db.ts`
- Create: `backend/tests/setup.ts`
- Create: `backend/tests/sanity.test.ts`
- Modify: `.gitignore` (ajouter `backend/.env.test`)

**Interfaces:**
- Produces:
  - `resetDb(): Promise<void>` — TRUNCATE toutes les tables applicatives, réutilisé par tous les tests.
  - `testPrisma: PrismaClient` — client Prisma pointant sur `DATABASE_URL` de `.env.test`.

- [ ] **Step 1: Installer Vitest et dotenv**

```bash
cd backend && npm install -D vitest@^2.1.0 dotenv@^16.4.5
```

- [ ] **Step 2: Créer `backend/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    globals: false,
    setupFiles: ["./tests/setup.ts"],
    include: ["tests/**/*.test.ts"],
    fileParallelism: false, // une seule DB de test partagée — pas d'exécution parallèle
    hookTimeout: 30000,
    testTimeout: 30000,
  },
});
```

- [ ] **Step 3: Créer `backend/.env.test.example`**

```dotenv
# Base Postgres DÉDIÉE aux tests — jamais la base de dev/prod.
# Créer la base : createdb sharinggo_test  (ou via le service postgres du docker-compose)
DATABASE_URL="postgresql://sharinggo:sharinggo@localhost:5432/sharinggo_test"
BOARDING_JWT_SECRET="test-only-boarding-secret-not-for-prod"
STRIPE_SECRET_KEY="sk_test_placeholder"
NODE_ENV="test"
```

- [ ] **Step 4: Ajouter `backend/.env.test` au `.gitignore`**

Ajouter la ligne `backend/.env.test` sous la section `# Env` de `.gitignore` (le fichier `.env.test` réel contient une URL de DB locale, on ne versionne que `.example`).

- [ ] **Step 5: Créer `backend/tests/helpers/db.ts`**

```ts
import { PrismaClient } from "@prisma/client";

export const testPrisma = new PrismaClient();

// Ordre inverse des dépendances FK. `RESTART IDENTITY CASCADE` vide tout proprement.
const TABLES = [
  "Credit",
  "Payment",
  "Reservation",
  "PendingReservation",
  "AuditLog",
  "WebhookEvent",
  "Trip",
  "Subscription",
  "Line",
  "OAuthAccount",
  "User",
];

export async function resetDb(): Promise<void> {
  const list = TABLES.map((t) => `"${t}"`).join(", ");
  await testPrisma.$executeRawUnsafe(`TRUNCATE TABLE ${list} RESTART IDENTITY CASCADE;`);
}
```

- [ ] **Step 6: Créer `backend/tests/setup.ts`**

```ts
import { config } from "dotenv";
import { afterAll, beforeEach } from "vitest";
import { resetDb, testPrisma } from "./helpers/db.js";

// Charger .env.test AVANT toute connexion Prisma.
config({ path: ".env.test" });

beforeEach(async () => {
  await resetDb();
});

afterAll(async () => {
  await testPrisma.$disconnect();
});
```

- [ ] **Step 7: Câbler les scripts de test dans `backend/package.json`**

Remplacer la ligne `"test": "echo \"No tests in S0-T1\" && exit 0",` par :

```json
    "test": "prisma migrate deploy && vitest run",
    "test:watch": "vitest",
```

- [ ] **Step 8: Créer le test sanity `backend/tests/sanity.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { testPrisma } from "./helpers/db.js";

describe("test infrastructure", () => {
  it("connects to the test database and starts empty", async () => {
    const userCount = await testPrisma.user.count();
    expect(userCount).toBe(0);
  });
});
```

- [ ] **Step 9: Créer la base de test et lancer**

```bash
cd backend
cp .env.test.example .env.test
# créer la base (adapter à ton setup local / docker)
createdb sharinggo_test || true
npm test
```
Expected: `prisma migrate deploy` applique les migrations, puis `sanity.test.ts` PASS (1 test).

- [ ] **Step 10: Commit**

```bash
git add backend/package.json backend/vitest.config.ts backend/.env.test.example backend/tests/ .gitignore
git commit -m "test(cascade-01): add Vitest + Postgres integration test foundation"
```

---

### Task 2: Migration DB — RefundStatus, CreditStatus, colonnes Reservation, table Credit

Ajoute le modèle de données du spec §5. Prérequis de toutes les tâches suivantes.

**Files:**
- Modify: `backend/prisma/schema.prisma` (enums, `Reservation`, `Credit`, `User`)
- Create: migration générée par `prisma migrate dev`
- Test: `backend/tests/cascade/schema.test.ts`

**Interfaces:**
- Produces (Prisma Client généré) :
  - `RefundStatus` = `NONE | PENDING | REFUNDED | CREDITED`
  - `CreditStatus` = `AVAILABLE | USED`
  - `Reservation.refundStatus`, `refundProcessedAt`, `refundProcessedByUserId`, `refundProcessedBy`, `credit`
  - `Credit { id, userId, user, sourceReservationId, sourceReservation, amount, status, usedOnReservationId, createdAt, expiresAt }`
  - `User.refundedReservations`, `User.credits`

- [ ] **Step 1: Ajouter les enums dans `schema.prisma`**

Après le bloc `enum PaymentStatus { … }` (vers schema.prisma:30), ajouter :

```prisma
enum RefundStatus {
  NONE
  PENDING
  REFUNDED
  CREDITED
}

enum CreditStatus {
  AVAILABLE
  USED
}
```

- [ ] **Step 2: Ajouter les colonnes sur `model Reservation`**

Dans `model Reservation`, après la ligne `payment Payment?` (schema.prisma:262), ajouter :

```prisma
  refundStatus            RefundStatus @default(NONE)
  refundProcessedAt       DateTime?
  refundProcessedByUserId String?
  refundProcessedBy       User?        @relation("RefundProcessedBy", fields: [refundProcessedByUserId], references: [id], onDelete: SetNull)
  credit                  Credit?
```

Et ajouter dans le bloc d'index de `Reservation` (près de `@@index([status])`) :

```prisma
  @@index([refundStatus])
```

- [ ] **Step 3: Ajouter le modèle `Credit`**

Après `model Payment { … }` (vers schema.prisma:323), ajouter :

```prisma
model Credit {
  id                  String       @id @default(cuid())
  userId              String
  user                User         @relation(fields: [userId], references: [id])
  sourceReservationId String       @unique
  sourceReservation   Reservation  @relation(fields: [sourceReservationId], references: [id])
  amount              Decimal      @db.Decimal(10, 2)
  status              CreditStatus @default(AVAILABLE)
  usedOnReservationId String?
  createdAt           DateTime     @default(now())
  expiresAt           DateTime?

  @@index([userId])
  @@index([status])
}
```

- [ ] **Step 4: Ajouter les relations réciproques sur `model User`**

Dans `model User` (après schema.prisma:103), ajouter les deux côtés inverses :

```prisma
  refundedReservations Reservation[] @relation("RefundProcessedBy")
  credits              Credit[]
```

- [ ] **Step 5: Générer la migration**

```bash
cd backend && npx prisma migrate dev --name cascade_01_refund_credit
```
Expected: migration créée sous `prisma/migrations/<timestamp>_cascade_01_refund_credit/`, Prisma Client régénéré, aucune erreur de validation de schéma (les relations `RefundProcessedBy` + `credit`/`credits` sont réciproques).

- [ ] **Step 6: Écrire le test de schéma `backend/tests/cascade/schema.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { RefundStatus, CreditStatus } from "@prisma/client";
import { testPrisma } from "../helpers/db.js";

describe("cascade-01 schema", () => {
  it("exposes RefundStatus and CreditStatus enums", () => {
    expect(RefundStatus.NONE).toBe("NONE");
    expect(RefundStatus.PENDING).toBe("PENDING");
    expect(RefundStatus.REFUNDED).toBe("REFUNDED");
    expect(RefundStatus.CREDITED).toBe("CREDITED");
    expect(CreditStatus.AVAILABLE).toBe("AVAILABLE");
    expect(CreditStatus.USED).toBe("USED");
  });

  it("defaults reservation.refundStatus to NONE", async () => {
    const user = await testPrisma.user.create({
      data: { email: "t@sharinggo.test", firstName: "T", lastName: "U", userType: "CONVOYEUR" },
    });
    const line = await testPrisma.line.create({
      data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" },
    });
    const trip = await testPrisma.trip.create({
      data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) },
    });
    const reservation = await testPrisma.reservation.create({
      data: { tripId: trip.id, userId: user.id },
    });
    expect(reservation.refundStatus).toBe(RefundStatus.NONE);
  });

  it("enforces one credit per source reservation (unique)", async () => {
    const user = await testPrisma.user.create({
      data: { email: "c@sharinggo.test", firstName: "C", lastName: "U", userType: "CONVOYEUR" },
    });
    const line = await testPrisma.line.create({
      data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" },
    });
    const trip = await testPrisma.trip.create({
      data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) },
    });
    const reservation = await testPrisma.reservation.create({
      data: { tripId: trip.id, userId: user.id },
    });
    await testPrisma.credit.create({
      data: { userId: user.id, sourceReservationId: reservation.id, amount: "18.00" },
    });
    await expect(
      testPrisma.credit.create({
        data: { userId: user.id, sourceReservationId: reservation.id, amount: "18.00" },
      })
    ).rejects.toThrow();
  });
});
```

> **Note pour l'implémenteur :** les seeds utilisent les vrais champs vérifiés contre `schema.prisma` :
> `Line { name, startCity, endCity }` (PAS origin/destination/priceCents), `User { email, firstName?, lastName?, userType }`,
> `Trip { lineId, departureTime }` (pas de prix sur Trip — le montant vit sur `Payment.amount`). Si un champ
> obligatoire manque à l'exécution, l'ajuster au schéma réel.

- [ ] **Step 7: Lancer les tests**

```bash
cd backend && npm test
```
Expected: `schema.test.ts` PASS (3 tests), `sanity.test.ts` PASS.

- [ ] **Step 8: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/migrations backend/tests/cascade/schema.test.ts
git commit -m "feat(cascade-01): add RefundStatus/CreditStatus, Credit table, reservation refund fields"
```

---

### Task 3: Helper `writeCascadeAuditLog` + cascade transactionnelle dans `cancelTrip()`

Cœur de la feature. `cancelTrip()` bascule les réservations liées et met en file `PENDING` les payées, le tout dans une transaction unique avec audit granulaire propageant l'erreur (échec log = rollback). `writeAuditLog` n'est PAS touché.

**Files:**
- Create: `backend/src/lib/cascade-audit-log.ts`
- Modify: `backend/src/modules/trips/trip-lifecycle.service.ts:160-187` (fonction `cancelTrip`)
- Modify: `backend/src/modules/trips/trip-lifecycle.constants.ts` (nouvelles actions d'audit)
- Test: `backend/tests/cascade/cancel-trip-cascade.test.ts`

**Interfaces:**
- Consumes: `RefundStatus` (Task 2), `ReservationStatus`, `PaymentStatus`, `TripLifecycleStatus` (Prisma).
- Produces:
  - `writeCascadeAuditLog(tx: Prisma.TransactionClient, input: { actorUserId?: string | null; action: string; targetType: string; targetId?: string; metadata?: Prisma.InputJsonValue }): Promise<void>` — fait `tx.auditLog.create`, ne catch pas (l'erreur propage et abort la tx).
  - `cancelTrip(tripId, input, actorUserId)` conserve sa signature actuelle et son type de retour `Promise<TripWithRelations>`.

- [ ] **Step 1: Écrire le test de cascade (échec attendu)**

`backend/tests/cascade/cancel-trip-cascade.test.ts` :

```ts
import { describe, expect, it, vi } from "vitest";
import { PaymentStatus, RefundStatus, ReservationStatus, TripLifecycleStatus } from "@prisma/client";
import { testPrisma } from "../helpers/db.js";
import { cancelTrip } from "../../src/modules/trips/trip-lifecycle.service.js";

// Helpers de seed — adapter aux champs réels des modèles.
async function seedTripWithReservations() {
  const admin = await testPrisma.user.create({
    data: { email: `a${Date.now()}@t.test`, firstName: "A", lastName: "D", userType: "ADMIN" },
  });
  const paxPaid = await testPrisma.user.create({
    data: { email: `p${Date.now()}@t.test`, firstName: "P", lastName: "Paid", userType: "CONVOYEUR" },
  });
  const paxUnpaid = await testPrisma.user.create({
    data: { email: `u${Date.now()}@t.test`, firstName: "U", lastName: "Unpaid", userType: "CONVOYEUR" },
  });
  const line = await testPrisma.line.create({
    data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" },
  });
  const trip = await testPrisma.trip.create({
    data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) },
  });
  const rPaid = await testPrisma.reservation.create({
    data: { tripId: trip.id, userId: paxPaid.id, status: ReservationStatus.CONFIRMED },
  });
  await testPrisma.payment.create({
    data: {
      userId: paxPaid.id,
      reservationId: rPaid.id,
      stripePaymentIntentId: `pi_${Date.now()}`,
      amount: "18.00",
      status: PaymentStatus.SUCCEEDED,
      type: "TICKET",
    },
  });
  const rUnpaid = await testPrisma.reservation.create({
    data: { tripId: trip.id, userId: paxUnpaid.id, status: ReservationStatus.PENDING },
  });
  return { admin, trip, rPaid, rUnpaid };
}

describe("cancelTrip cascade", () => {
  it("cancels reservations and flags paid ones PENDING, unpaid NONE", async () => {
    const { admin, trip, rPaid, rUnpaid } = await seedTripWithReservations();

    await cancelTrip(trip.id, { reason: "weather" }, admin.id);

    const updatedTrip = await testPrisma.trip.findUniqueOrThrow({ where: { id: trip.id } });
    expect(updatedTrip.lifecycleStatus).toBe(TripLifecycleStatus.CANCELLED);

    const paid = await testPrisma.reservation.findUniqueOrThrow({ where: { id: rPaid.id } });
    expect(paid.status).toBe(ReservationStatus.CANCELED);
    expect(paid.refundStatus).toBe(RefundStatus.PENDING);

    const unpaid = await testPrisma.reservation.findUniqueOrThrow({ where: { id: rUnpaid.id } });
    expect(unpaid.status).toBe(ReservationStatus.CANCELED);
    expect(unpaid.refundStatus).toBe(RefundStatus.NONE);
  });

  it("writes one audit log per impacted reservation plus one for the trip", async () => {
    const { admin, trip } = await seedTripWithReservations();
    await cancelTrip(trip.id, { reason: "weather" }, admin.id);

    const resLogs = await testPrisma.auditLog.count({
      where: { action: "RESERVATION_CANCELLED_BY_TRIP", targetType: "Reservation" },
    });
    const tripLogs = await testPrisma.auditLog.count({
      where: { action: "TRIP_CANCELLED", targetType: "Trip", targetId: trip.id },
    });
    expect(resLogs).toBe(2);
    expect(tripLogs).toBe(1);
  });

  it("does not trigger any Stripe call on cancellation", async () => {
    const stripeSpy = vi.fn();
    // getStripeClient ne doit pas être invoqué par la cascade.
    const { admin, trip } = await seedTripWithReservations();
    await cancelTrip(trip.id, { reason: "weather" }, admin.id);
    expect(stripeSpy).not.toHaveBeenCalled();
  });

  it("rejects double cancellation with 409 and does not re-cascade", async () => {
    const { admin, trip, rPaid } = await seedTripWithReservations();
    await cancelTrip(trip.id, { reason: "weather" }, admin.id);
    await expect(cancelTrip(trip.id, { reason: "again" }, admin.id)).rejects.toMatchObject({
      statusCode: 409,
      code: "INVALID_LIFECYCLE_TRANSITION",
    });
    // refundStatus inchangé après le 2e appel refusé
    const paid = await testPrisma.reservation.findUniqueOrThrow({ where: { id: rPaid.id } });
    expect(paid.refundStatus).toBe(RefundStatus.PENDING);
  });
});
```

- [ ] **Step 2: Lancer le test — vérifier qu'il échoue**

```bash
cd backend && npx vitest run tests/cascade/cancel-trip-cascade.test.ts
```
Expected: FAIL (la cascade n'existe pas encore — réservations restent `CONFIRMED`/`PENDING`, pas de logs `RESERVATION_CANCELLED_BY_TRIP`).

- [ ] **Step 3: Créer le helper `backend/src/lib/cascade-audit-log.ts`**

```ts
import type { Prisma } from "@prisma/client";

export interface CascadeAuditLogInput {
  actorUserId?: string | null;
  action: string;
  targetType: string;
  targetId?: string;
  metadata?: Prisma.InputJsonValue;
}

/**
 * Audit log écrit DANS une transaction. Contrairement à writeAuditLog (lib/audit-log.ts),
 * une erreur d'insert n'est PAS avalée : elle propage et fait rollback la transaction.
 * Réservé aux écritures transactionnelles de CASCADE-01.
 */
export async function writeCascadeAuditLog(
  tx: Prisma.TransactionClient,
  input: CascadeAuditLogInput
): Promise<void> {
  await tx.auditLog.create({
    data: {
      actorUserId: input.actorUserId ?? null,
      action: input.action,
      targetType: input.targetType,
      targetId: input.targetId,
      metadata: input.metadata ?? undefined,
    },
  });
}
```

- [ ] **Step 4: Ajouter les constantes d'action dans `trip-lifecycle.constants.ts`**

Ajouter à l'objet `TRIP_LIFECYCLE_AUDIT_ACTIONS` (ou créer la constante à côté) :

```ts
// Action émise pour chaque réservation impactée par l'annulation d'un trajet.
export const RESERVATION_CANCELLED_BY_TRIP = "RESERVATION_CANCELLED_BY_TRIP";
```

> Vérifier le nom exact de la constante `TRIP_CANCELLED` déjà présente dans ce fichier et la réutiliser (ne pas dupliquer).

- [ ] **Step 5: Réécrire `cancelTrip()` avec la cascade transactionnelle**

Remplacer le corps de `cancelTrip` (`trip-lifecycle.service.ts:160-187`) par :

```ts
export async function cancelTrip(
  tripId: string,
  input: CancelTripInput,
  actorUserId: string
): Promise<TripWithRelations> {
  const trip = await getTripOrThrow(tripId);
  assertTripActive(trip);
  assertTransitionAllowed(trip.lifecycleStatus, TripLifecycleStatus.CANCELLED);

  const now = new Date();

  await prisma.$transaction(async (tx) => {
    await tx.trip.update({
      where: { id: tripId },
      data: {
        lifecycleStatus: TripLifecycleStatus.CANCELLED,
        cancelledAt: now,
        cancellationReason: input.reason,
        lifecycleUpdatedByUserId: actorUserId,
      },
    });

    const reservations = await tx.reservation.findMany({
      where: {
        tripId,
        status: { in: [ReservationStatus.PENDING, ReservationStatus.CONFIRMED] },
      },
      include: { payment: true },
    });

    for (const reservation of reservations) {
      const hasCapturedPayment =
        reservation.payment?.status === PaymentStatus.SUCCEEDED &&
        reservation.payment.stripePaymentIntentId != null;

      await tx.reservation.update({
        where: { id: reservation.id },
        data: {
          status: ReservationStatus.CANCELED,
          refundStatus: hasCapturedPayment ? RefundStatus.PENDING : RefundStatus.NONE,
        },
      });

      await writeCascadeAuditLog(tx, {
        actorUserId,
        action: RESERVATION_CANCELLED_BY_TRIP,
        targetType: "Reservation",
        targetId: reservation.id,
        metadata: { tripId, refundStatus: hasCapturedPayment ? "PENDING" : "NONE", hadPayment: hasCapturedPayment },
      });
    }

    await writeCascadeAuditLog(tx, {
      actorUserId,
      action: TRIP_LIFECYCLE_AUDIT_ACTIONS.CANCELLED,
      targetType: "Trip",
      targetId: tripId,
      metadata: {
        previousStatus: trip.lifecycleStatus,
        cancelledAt: now.toISOString(),
        reason: input.reason,
        impactedReservations: reservations.length,
      },
    });
  });

  return getTripOrThrow(tripId);
}
```

Ajouter les imports en tête du fichier :

```ts
import { PaymentStatus, RefundStatus, ReservationStatus, TripLifecycleStatus, type Prisma } from "@prisma/client";
import { writeCascadeAuditLog } from "../../lib/cascade-audit-log.js";
import { RESERVATION_CANCELLED_BY_TRIP } from "./trip-lifecycle.constants.js";
```

> **Note :** le test `TRIP_CANCELLED` attend `action: "TRIP_CANCELLED"`. Vérifier que `TRIP_LIFECYCLE_AUDIT_ACTIONS.CANCELLED === "TRIP_CANCELLED"` ; sinon aligner le test sur la vraie valeur de la constante.

- [ ] **Step 6: Lancer le test — vérifier qu'il passe**

```bash
cd backend && npx vitest run tests/cascade/cancel-trip-cascade.test.ts
```
Expected: PASS (4 tests).

- [ ] **Step 7: Lint + typecheck**

```bash
cd backend && npm run lint && npx tsc --noEmit
```
Expected: exit 0, aucun warning.

- [ ] **Step 8: Commit**

```bash
git add backend/src/lib/cascade-audit-log.ts backend/src/modules/trips/trip-lifecycle.service.ts backend/src/modules/trips/trip-lifecycle.constants.ts backend/tests/cascade/cancel-trip-cascade.test.ts
git commit -m "feat(cascade-01): cascade trip cancellation to reservations with transactional audit"
```

---

### Task 4: Action admin « Rembourser » (route + service + FOR UPDATE + Stripe idempotent)

**Files:**
- Modify: `backend/src/modules/admin/admin-reservations.service.ts` (ajout `refundReservation`)
- Modify: `backend/src/modules/admin/admin-reservations.controller.ts` (ajout handler)
- Modify: `backend/src/modules/admin/admin.routes.ts` (nouvelle route)
- Test: `backend/tests/cascade/admin-refund.test.ts`

**Interfaces:**
- Consumes: `getStripeClient()` (`payments/stripe.service.ts`), `writeCascadeAuditLog` (Task 3), `RefundStatus`, `PaymentStatus`.
- Produces:
  - `refundReservation(reservationId: string, actorUserId: string): Promise<AdminReservationView>` (type de retour = celui de `serializeAdminReservation`).
  - Route `POST /api/admin/reservations/:id/refund`.

- [ ] **Step 1: Écrire le test (échec attendu)**

`backend/tests/cascade/admin-refund.test.ts` :

```ts
import { describe, expect, it, vi, beforeEach } from "vitest";
import { PaymentStatus, RefundStatus, ReservationStatus } from "@prisma/client";
import { testPrisma } from "../helpers/db.js";

// Mock du client Stripe AVANT import du service.
const refundsCreate = vi.fn(async () => ({ id: "re_test_123", status: "succeeded" }));
vi.mock("../../src/modules/payments/stripe.service.js", () => ({
  getStripeClient: () => ({ refunds: { create: refundsCreate } }),
}));

const { refundReservation } = await import("../../src/modules/admin/admin-reservations.service.js");

async function seedPendingRefund() {
  const admin = await testPrisma.user.create({
    data: { email: `a${Date.now()}@t.test`, firstName: "A", lastName: "D", userType: "ADMIN" },
  });
  const pax = await testPrisma.user.create({
    data: { email: `p${Date.now()}@t.test`, firstName: "P", lastName: "X", userType: "CONVOYEUR" },
  });
  const line = await testPrisma.line.create({
    data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" },
  });
  const trip = await testPrisma.trip.create({
    data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) },
  });
  const reservation = await testPrisma.reservation.create({
    data: { tripId: trip.id, userId: pax.id, status: ReservationStatus.CANCELED, refundStatus: RefundStatus.PENDING },
  });
  await testPrisma.payment.create({
    data: {
      userId: pax.id, reservationId: reservation.id,
      stripePaymentIntentId: `pi_${Date.now()}`, amount: "18.00",
      status: PaymentStatus.SUCCEEDED, type: "TICKET",
    },
  });
  return { admin, reservation };
}

describe("refundReservation", () => {
  beforeEach(() => refundsCreate.mockClear());

  it("refunds via Stripe once with deterministic idempotency key and sets REFUNDED", async () => {
    const { admin, reservation } = await seedPendingRefund();
    await refundReservation(reservation.id, admin.id);

    expect(refundsCreate).toHaveBeenCalledTimes(1);
    const [, opts] = refundsCreate.mock.calls[0];
    expect(opts).toMatchObject({ idempotencyKey: `refund_${reservation.id}` });

    const updated = await testPrisma.reservation.findUniqueOrThrow({ where: { id: reservation.id } });
    expect(updated.refundStatus).toBe(RefundStatus.REFUNDED);
    expect(updated.refundProcessedByUserId).toBe(admin.id);

    const payment = await testPrisma.payment.findFirstOrThrow({ where: { reservationId: reservation.id } });
    expect(payment.status).toBe(PaymentStatus.REFUNDED);
  });

  it("rejects with 409 when not PENDING", async () => {
    const { admin, reservation } = await seedPendingRefund();
    await testPrisma.reservation.update({ where: { id: reservation.id }, data: { refundStatus: RefundStatus.REFUNDED } });
    await expect(refundReservation(reservation.id, admin.id)).rejects.toMatchObject({
      statusCode: 409, code: "REFUND_NOT_PENDING",
    });
    expect(refundsCreate).not.toHaveBeenCalled();
  });

  it("rejects with 422 when no payment intent", async () => {
    const { admin, reservation } = await seedPendingRefund();
    await testPrisma.payment.deleteMany({ where: { reservationId: reservation.id } });
    await expect(refundReservation(reservation.id, admin.id)).rejects.toMatchObject({
      statusCode: 422, code: "NO_PAYMENT_INTENT",
    });
    expect(refundsCreate).not.toHaveBeenCalled();
  });

  it("returns 502 and keeps PENDING when Stripe fails", async () => {
    const { admin, reservation } = await seedPendingRefund();
    refundsCreate.mockRejectedValueOnce(new Error("stripe down"));
    await expect(refundReservation(reservation.id, admin.id)).rejects.toMatchObject({
      statusCode: 502, code: "STRIPE_REFUND_FAILED",
    });
    const updated = await testPrisma.reservation.findUniqueOrThrow({ where: { id: reservation.id } });
    expect(updated.refundStatus).toBe(RefundStatus.PENDING);
    const payment = await testPrisma.payment.findFirstOrThrow({ where: { reservationId: reservation.id } });
    expect(payment.status).toBe(PaymentStatus.SUCCEEDED);
  });
});
```

- [ ] **Step 2: Lancer — échec attendu**

```bash
cd backend && npx vitest run tests/cascade/admin-refund.test.ts
```
Expected: FAIL (`refundReservation` n'existe pas).

- [ ] **Step 3: Implémenter `refundReservation` dans `admin-reservations.service.ts`**

Ajouter les imports :

```ts
import { PaymentStatus, RefundStatus } from "@prisma/client";
import { getStripeClient } from "../payments/stripe.service.js";
import { writeCascadeAuditLog } from "../../lib/cascade-audit-log.js";
```

Ajouter la fonction :

```ts
export async function refundReservation(reservationId: string, actorUserId: string) {
  // Étape A — tx courte : verrou pessimiste + garde d'état.
  const guard = await prisma.$transaction(async (tx) => {
    // FOR UPDATE sérialise les requêtes concurrentes sur cette ligne.
    const rows = await tx.$queryRaw<Array<{ id: string; refundStatus: RefundStatus }>>`
      SELECT id, "refundStatus" FROM "Reservation" WHERE id = ${reservationId} FOR UPDATE
    `;
    const row = rows[0];
    if (!row) throw new AppError("Reservation not found", 404, "RESERVATION_NOT_FOUND");
    if (row.refundStatus !== RefundStatus.PENDING) {
      throw new AppError("Reservation is not pending refund", 409, "REFUND_NOT_PENDING");
    }
    const payment = await tx.payment.findUnique({ where: { reservationId } });
    if (!payment?.stripePaymentIntentId) {
      throw new AppError("No payment intent to refund", 422, "NO_PAYMENT_INTENT");
    }
    return { paymentIntentId: payment.stripePaymentIntentId };
  });

  // Étape B — HORS transaction : appel Stripe idempotent.
  try {
    await getStripeClient().refunds.create(
      { payment_intent: guard.paymentIntentId },
      { idempotencyKey: `refund_${reservationId}` }
    );
  } catch {
    throw new AppError("Stripe refund failed", 502, "STRIPE_REFUND_FAILED");
  }

  // Étape C — tx courte : commit REFUNDED + payment + audit.
  await prisma.$transaction(async (tx) => {
    await tx.reservation.update({
      where: { id: reservationId },
      data: {
        refundStatus: RefundStatus.REFUNDED,
        refundProcessedAt: new Date(),
        refundProcessedByUserId: actorUserId,
      },
    });
    await tx.payment.updateMany({
      where: { reservationId },
      data: { status: PaymentStatus.REFUNDED },
    });
    await writeCascadeAuditLog(tx, {
      actorUserId,
      action: "RESERVATION_REFUNDED",
      targetType: "Reservation",
      targetId: reservationId,
      metadata: { paymentIntentId: guard.paymentIntentId },
    });
  });

  const updated = await prisma.reservation.findUniqueOrThrow({
    where: { id: reservationId },
    include: reservationInclude,
  });
  return serializeAdminReservation(updated);
}
```

- [ ] **Step 4: Ajouter le handler dans `admin-reservations.controller.ts`**

```ts
export async function refundAdminReservationHandler(req: Request, res: Response): Promise<void> {
  const { id } = parseQuery(adminIdParamSchema, { id: req.params.id });
  const reservation = await adminReservationsService.refundReservation(id, req.user!.id);
  res.status(200).json(reservation);
}
```

- [ ] **Step 5: Ajouter la route dans `admin.routes.ts`**

Importer `refundAdminReservationHandler` (bloc d'import `admin-reservations.controller.js`), puis après la route `/reservations/:id/cancel` :

```ts
adminOperationsRouter.post(
  "/reservations/:id/refund",
  ...adminMiddleware,
  asyncHandler(refundAdminReservationHandler)
);
```

- [ ] **Step 6: Lancer — vérifier que ça passe**

```bash
cd backend && npx vitest run tests/cascade/admin-refund.test.ts
```
Expected: PASS (4 tests).

- [ ] **Step 7: Lint + typecheck**

```bash
cd backend && npm run lint && npx tsc --noEmit
```
Expected: exit 0.

- [ ] **Step 8: Commit**

```bash
git add backend/src/modules/admin/admin-reservations.service.ts backend/src/modules/admin/admin-reservations.controller.ts backend/src/modules/admin/admin.routes.ts backend/tests/cascade/admin-refund.test.ts
git commit -m "feat(cascade-01): admin refund action with FOR UPDATE guard and idempotent Stripe refund"
```

---

### Task 5: Action admin « Créditer un avoir » (route + service)

**Files:**
- Modify: `backend/src/modules/admin/admin-reservations.service.ts` (ajout `creditReservation`)
- Modify: `backend/src/modules/admin/admin-reservations.controller.ts` (handler)
- Modify: `backend/src/modules/admin/admin.routes.ts` (route)
- Test: `backend/tests/cascade/admin-credit.test.ts`

**Interfaces:**
- Consumes: `RefundStatus`, `CreditStatus`, `writeCascadeAuditLog`.
- Produces:
  - `creditReservation(reservationId: string, actorUserId: string): Promise<AdminReservationView>`.
  - Route `POST /api/admin/reservations/:id/credit`.

- [ ] **Step 1: Écrire le test (échec attendu)**

`backend/tests/cascade/admin-credit.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import { CreditStatus, PaymentStatus, RefundStatus, ReservationStatus } from "@prisma/client";
import { testPrisma } from "../helpers/db.js";
import { creditReservation } from "../../src/modules/admin/admin-reservations.service.js";

async function seedPendingRefund() {
  const admin = await testPrisma.user.create({
    data: { email: `a${Date.now()}@t.test`, firstName: "A", lastName: "D", userType: "ADMIN" },
  });
  const pax = await testPrisma.user.create({
    data: { email: `p${Date.now()}@t.test`, firstName: "P", lastName: "X", userType: "CONVOYEUR" },
  });
  const line = await testPrisma.line.create({ data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" } });
  const trip = await testPrisma.trip.create({ data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) } });
  const reservation = await testPrisma.reservation.create({
    data: { tripId: trip.id, userId: pax.id, status: ReservationStatus.CANCELED, refundStatus: RefundStatus.PENDING },
  });
  await testPrisma.payment.create({
    data: {
      userId: pax.id, reservationId: reservation.id, stripePaymentIntentId: `pi_${Date.now()}`,
      amount: "18.00", status: PaymentStatus.SUCCEEDED, type: "TICKET",
    },
  });
  return { admin, pax, reservation };
}

describe("creditReservation", () => {
  it("creates a credit and sets CREDITED with amount from payment", async () => {
    const { admin, pax, reservation } = await seedPendingRefund();
    await creditReservation(reservation.id, admin.id);

    const updated = await testPrisma.reservation.findUniqueOrThrow({ where: { id: reservation.id } });
    expect(updated.refundStatus).toBe(RefundStatus.CREDITED);
    expect(updated.refundProcessedByUserId).toBe(admin.id);

    const credit = await testPrisma.credit.findUniqueOrThrow({ where: { sourceReservationId: reservation.id } });
    expect(credit.userId).toBe(pax.id);
    expect(credit.status).toBe(CreditStatus.AVAILABLE);
    expect(credit.amount.toString()).toBe("18");
    expect(credit.expiresAt).toBeNull();
  });

  it("rejects with 409 when not PENDING", async () => {
    const { admin, reservation } = await seedPendingRefund();
    await testPrisma.reservation.update({ where: { id: reservation.id }, data: { refundStatus: RefundStatus.CREDITED } });
    await expect(creditReservation(reservation.id, admin.id)).rejects.toMatchObject({
      statusCode: 409, code: "REFUND_NOT_PENDING",
    });
  });
});
```

- [ ] **Step 2: Lancer — échec attendu**

```bash
cd backend && npx vitest run tests/cascade/admin-credit.test.ts
```
Expected: FAIL (`creditReservation` n'existe pas).

- [ ] **Step 3: Implémenter `creditReservation`**

Ajouter dans `admin-reservations.service.ts` (import `CreditStatus` en plus) :

```ts
export async function creditReservation(reservationId: string, actorUserId: string) {
  const updated = await prisma.$transaction(async (tx) => {
    const rows = await tx.$queryRaw<Array<{ id: string; refundStatus: RefundStatus }>>`
      SELECT id, "refundStatus" FROM "Reservation" WHERE id = ${reservationId} FOR UPDATE
    `;
    const row = rows[0];
    if (!row) throw new AppError("Reservation not found", 404, "RESERVATION_NOT_FOUND");
    if (row.refundStatus !== RefundStatus.PENDING) {
      throw new AppError("Reservation is not pending refund", 409, "REFUND_NOT_PENDING");
    }

    const payment = await tx.payment.findUnique({ where: { reservationId } });
    if (!payment) throw new AppError("No payment to credit", 422, "NO_PAYMENT_INTENT");

    const reservation = await tx.reservation.findUniqueOrThrow({ where: { id: reservationId } });

    const credit = await tx.credit.create({
      data: {
        userId: reservation.userId,
        sourceReservationId: reservationId,
        amount: payment.amount,
        status: CreditStatus.AVAILABLE,
        // expiresAt laissé null (décision V1)
      },
    });

    await tx.reservation.update({
      where: { id: reservationId },
      data: {
        refundStatus: RefundStatus.CREDITED,
        refundProcessedAt: new Date(),
        refundProcessedByUserId: actorUserId,
      },
    });

    await writeCascadeAuditLog(tx, {
      actorUserId,
      action: "RESERVATION_CREDITED",
      targetType: "Reservation",
      targetId: reservationId,
      metadata: { creditId: credit.id, amount: payment.amount.toString() },
    });

    return tx.reservation.findUniqueOrThrow({ where: { id: reservationId }, include: reservationInclude });
  });

  return serializeAdminReservation(updated);
}
```

- [ ] **Step 4: Handler + route**

Dans `admin-reservations.controller.ts` :

```ts
export async function creditAdminReservationHandler(req: Request, res: Response): Promise<void> {
  const { id } = parseQuery(adminIdParamSchema, { id: req.params.id });
  const reservation = await adminReservationsService.creditReservation(id, req.user!.id);
  res.status(200).json(reservation);
}
```

Dans `admin.routes.ts` (import + route après `/refund`) :

```ts
adminOperationsRouter.post(
  "/reservations/:id/credit",
  ...adminMiddleware,
  asyncHandler(creditAdminReservationHandler)
);
```

- [ ] **Step 5: Lancer — vérifier**

```bash
cd backend && npx vitest run tests/cascade/admin-credit.test.ts
```
Expected: PASS (2 tests).

- [ ] **Step 6: Lint + typecheck + commit**

```bash
cd backend && npm run lint && npx tsc --noEmit
git add backend/src/modules/admin/ backend/tests/cascade/admin-credit.test.ts
git commit -m "feat(cascade-01): admin credit action creating a traceable credit"
```

---

### Task 6: Liste filtrée `?refundStatus=PENDING` + sérialisation refund

Étend la liste admin existante et expose les champs refund (PI tronqué) dans la sérialisation.

**Files:**
- Modify: `backend/src/modules/admin/admin.schemas.ts` (champ `refundStatus` optionnel)
- Modify: `backend/src/modules/admin/admin-reservations.service.ts` (`buildWhere`)
- Modify: `backend/src/modules/admin/admin.serializers.ts` (champs refund)
- Test: `backend/tests/cascade/admin-list-filter.test.ts`

**Interfaces:**
- Consumes: `RefundStatus`, `serializeAdminReservation` existant, `stripeShortRef` (`admin.serializers.ts`).
- Produces: `ListAdminReservationsQuery.refundStatus?: RefundStatus`; la vue sérialisée inclut `refundStatus`, `refundProcessedAt`, `refundProcessedBy`.

- [ ] **Step 1: Écrire le test (échec attendu)**

`backend/tests/cascade/admin-list-filter.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import { RefundStatus, ReservationStatus } from "@prisma/client";
import { testPrisma } from "../helpers/db.js";
import { listAdminReservations } from "../../src/modules/admin/admin-reservations.service.js";

describe("listAdminReservations refundStatus filter", () => {
  it("returns only PENDING when filtered", async () => {
    const pax = await testPrisma.user.create({
      data: { email: `p${Date.now()}@t.test`, firstName: "P", lastName: "X", userType: "CONVOYEUR" },
    });
    const line = await testPrisma.line.create({ data: { name: "Châlons ↔ Vatry", startCity: "Châlons-en-Champagne", endCity: "Paris-Vatry" } });
    const trip = await testPrisma.trip.create({ data: { lineId: line.id, departureTime: new Date(Date.now() + 86400000) } });

    await testPrisma.reservation.create({
      data: { tripId: trip.id, userId: pax.id, status: ReservationStatus.CANCELED, refundStatus: RefundStatus.PENDING },
    });
    await testPrisma.reservation.create({
      data: { tripId: trip.id, userId: pax.id, status: ReservationStatus.CONFIRMED, refundStatus: RefundStatus.NONE },
    });

    const result = await listAdminReservations({ refundStatus: RefundStatus.PENDING, limit: 50, offset: 0 } as never);
    expect(result.reservations).toHaveLength(1);
    expect(result.reservations[0].refundStatus).toBe(RefundStatus.PENDING);
  });
});
```

- [ ] **Step 2: Lancer — échec attendu**

```bash
cd backend && npx vitest run tests/cascade/admin-list-filter.test.ts
```
Expected: FAIL (filtre inexistant, `refundStatus` absent de la sérialisation).

- [ ] **Step 3: Étendre le schéma de query**

Dans `admin.schemas.ts`, ajouter à `listAdminReservationsQuerySchema` :

```ts
  refundStatus: z.nativeEnum(RefundStatus).optional(),
```

(importer `RefundStatus` depuis `@prisma/client` et `z` si pas déjà présent.)

- [ ] **Step 4: Ajouter la clause `where`**

Dans `buildWhere` (`admin-reservations.service.ts`), après le bloc `if (query.tripId)` :

```ts
  if (query.refundStatus) {
    where.refundStatus = query.refundStatus;
  }
```

- [ ] **Step 5: Exposer les champs refund dans le sérialiseur**

Dans `serializeAdminReservation` (`admin.serializers.ts`), ajouter à l'objet retourné :

```ts
    refundStatus: reservation.refundStatus,
    refundProcessedAt: reservation.refundProcessedAt,
    refundProcessedBy: reservation.refundProcessedBy
      ? { id: reservation.refundProcessedBy.id, name: `${reservation.refundProcessedBy.firstName} ${reservation.refundProcessedBy.lastName}` }
      : null,
```

> Le `stripePaymentIntentId` est déjà tronqué via `stripeShortRef()` (admin.serializers.ts:53) — ne pas exposer le PI complet. Vérifier que `reservationInclude` inclut `refundProcessedBy` : ajouter `refundProcessedBy: true` à `reservationInclude` dans `admin-reservations.service.ts` (sinon la relation est absente à la sérialisation).

- [ ] **Step 6: Lancer + lint + typecheck**

```bash
cd backend && npx vitest run tests/cascade/admin-list-filter.test.ts && npm run lint && npx tsc --noEmit
```
Expected: PASS (1 test), exit 0.

- [ ] **Step 7: Commit**

```bash
git add backend/src/modules/admin/ backend/tests/cascade/admin-list-filter.test.ts
git commit -m "feat(cascade-01): filter admin reservations by refundStatus and expose refund fields"
```

---

---

## 🚦 CHECKPOINT — Validation du socle backend (après Task 6, avant Task 7)

**Porte obligatoire.** Le backend complet (Tasks 1-6) doit être validé par le CTO **avant** de démarrer
le front, qui s'appuie dessus. Le contrôleur d'exécution s'arrête ici et présente :

- Récapitulatif des commits Tasks 1-6.
- Résultat de `cd backend && npm test` (toute la suite cascade + sanity + schema doit être verte).
- Résultat de `cd backend && npm run lint && npx tsc --noEmit` (exit 0).
- Liste des endpoints livrés : `GET …?refundStatus=PENDING`, `POST …/refund`, `POST …/credit`, et la cascade `cancelTrip()`.

**Ne pas dispatcher la Task 7 tant que le CTO n'a pas validé ce checkpoint.** Si le CTO demande des
ajustements backend, ils sont traités ici avant d'ouvrir le front.

---

### Task 7: Front admin — vue « Annulations à traiter » + modal + cas 409

Vue back-office listant les réservations `PENDING`, avec les deux actions, la confirmation, et la bascule 409 « déjà traité » + refresh auto (spec §8.1).

**Files:**
- Create: `frontend/src/pages/RefundQueuePage.tsx` (suivre le pattern de `frontend/src/pages/ReservationsPage.tsx` — même arbo, pas de sous-dossier `admin/`)
- Create: `frontend/src/components/RefundActionModal.tsx` (adapter à l'emplacement réel des composants — repéré au Step 1)
- Modify: le fichier de routing admin (ajout route `/refunds`) et le menu de navigation
- Modify: le client API admin (ajout `listPendingRefunds`, `refundReservation`, `creditReservation`)

> **Modèles à suivre :** `frontend/src/pages/ReservationsPage.tsx` (page liste réservations, déjà branchée sur `/api/admin/reservations`) et `frontend/src/pages/PaymentsPage.tsx`. Le Step 1 repère le client API et le routing exacts.

**Interfaces:**
- Consumes: endpoints `GET /api/admin/reservations?refundStatus=PENDING`, `POST /api/admin/reservations/:id/refund`, `POST /api/admin/reservations/:id/credit`.
- Produces: page listée dans la navigation admin ; le composant refetch la liste après chaque action ou 409.

- [ ] **Step 1: Repérer les patterns admin existants**

```bash
grep -rn "api/admin/reservations" frontend/src | head
# repérer le client API (apiGet/apiPost ou équivalent) :
grep -rn "export.*function.*api\|fetch(\|axios" frontend/src/lib frontend/src/api 2>/dev/null | head
# repérer le routing et le menu :
grep -rln "ReservationsPage\|createBrowserRouter\|<Route" frontend/src | head
```
Modèle de page liste : `frontend/src/pages/ReservationsPage.tsx`. Respecter l'UX (noir #000, vert #22c55e — DESIGN.md).

- [ ] **Step 2: Ajouter les appels API admin**

Dans le client API admin (fichier repéré au Step 1), ajouter :

```ts
export async function listPendingRefunds() {
  return apiGet(`/api/admin/reservations?refundStatus=PENDING`);
}
export async function refundReservation(id: string) {
  return apiPost(`/api/admin/reservations/${id}/refund`, {});
}
export async function creditReservation(id: string) {
  return apiPost(`/api/admin/reservations/${id}/credit`, {});
}
```

> Adapter `apiGet`/`apiPost` aux helpers réels du projet (repérés au Step 1). Le POST doit propager le statut HTTP pour distinguer 409 des autres erreurs.

- [ ] **Step 3: Créer le modal `RefundActionModal.tsx`**

Composant à deux états (spec §8.1, maquette validée) :
- **Nominal :** titre « Rembourser / Créditer cette réservation ? », rappel passager + trajet + montant + PI tronqué, boutons Annuler / Confirmer.
- **Cas 409 :** au retour d'une réponse `409 REFUND_NOT_PENDING`, basculer sur « Déjà traité entre-temps » (ton ambré, pas rouge), afficher le statut réel si renvoyé, et un bouton « Rafraîchir la liste » qui appelle `onRefresh()`.

```tsx
type ActionKind = "refund" | "credit";
type ModalState = "confirm" | "already-processed";

interface Props {
  reservation: { id: string; passengerName: string; tripLabel: string; amount: string; piRef: string };
  kind: ActionKind;
  onConfirm: (id: string, kind: ActionKind) => Promise<{ ok: true } | { ok: false; conflict: boolean }>;
  onClose: () => void;
  onRefresh: () => void;
}
```

Logique de `handleConfirm` : appeler `onConfirm` ; si résultat `ok` → toast succès + `onRefresh()` + `onClose()` ; si `conflict` (409) → `setState("already-processed")` ; sinon → toast erreur générique.

- [ ] **Step 4: Créer la page `RefundQueue.tsx`**

Table dense (colonnes : passager · trajet · montant · payé le / PI tronqué · actions Rembourser + Créditer). Au montage, `listPendingRefunds()`. Chaque bouton ouvre `RefundActionModal` avec le `kind` correspondant. `onRefresh` = refetch de la liste. Une réservation traitée disparaît de la liste (elle n'est plus `PENDING`).

- [ ] **Step 5: Router + navigation**

Ajouter la route `/admin/refunds` → `RefundQueue`, et une entrée « Annulations à traiter » dans la navigation admin (suivant le pattern des autres pages admin repéré au Step 1).

- [ ] **Step 6: Build front**

```bash
cd frontend && npm run build
```
Expected: build OK, aucune erreur TS.

- [ ] **Step 7: Vérification manuelle (verify)**

Lancer l'admin en dev, se connecter en admin, ouvrir « Annulations à traiter », vérifier : la liste charge les `PENDING`, le modal de confirmation s'affiche, un clic Rembourser sur une réservation déjà traitée (simuler en changeant le statut en DB entre l'ouverture et le clic) bascule sur « Déjà traité » + refresh. Capturer une preuve (screenshot).

- [ ] **Step 8: Commit**

```bash
git add frontend/src
git commit -m "feat(cascade-01): admin refund queue view with confirmation modal and 409 handling"
```

---

### Task 8: Statut in-app passager (3 états)

Expose `refundStatus` côté passager et affiche les libellés du spec §8.2.

**Files:**
- Modify: le sérialiseur de réservation passager (repérer dans `backend/src/modules/reservations/` ou `transport/`)
- Modify: le composant passager affichant le statut d'une réservation (frontend passager)
- Test: `backend/tests/cascade/passenger-serializer.test.ts`

**Interfaces:**
- Consumes: `RefundStatus`.
- Produces: le payload passager d'une réservation inclut `refundStatus`; le front passager mappe les 3 états en libellés.

- [ ] **Step 1: Repérer le sérialiseur passager**

```bash
grep -rn "serialize.*[Rr]eservation\|reservation.*status" backend/src/modules/reservations backend/src/modules/transport | head
```

- [ ] **Step 2: Écrire le test (échec attendu)**

`backend/tests/cascade/passenger-serializer.test.ts` — vérifier que le sérialiseur passager inclut `refundStatus` pour une réservation `CANCELED`/`PENDING`. Adapter le nom de la fonction sérialiseur repérée au Step 1.

```ts
import { describe, expect, it } from "vitest";
import { RefundStatus, ReservationStatus } from "@prisma/client";
// import { serializePassengerReservation } from "...";  // repérer au Step 1

describe("passenger reservation serializer", () => {
  it("exposes refundStatus", () => {
    const view = /* serializePassengerReservation */ ((r: unknown) => r)({
      status: ReservationStatus.CANCELED,
      refundStatus: RefundStatus.PENDING,
    });
    expect((view as { refundStatus: string }).refundStatus).toBe(RefundStatus.PENDING);
  });
});
```

- [ ] **Step 3: Ajouter `refundStatus` au sérialiseur passager**

Ajouter `refundStatus: reservation.refundStatus` à l'objet retourné par le sérialiseur passager repéré.

- [ ] **Step 4: Mapper les libellés côté front passager**

Dans le composant passager affichant le statut, ajouter le mapping (spec §8.2) :

```ts
function refundLabel(status: string, refundStatus: string): string | null {
  if (refundStatus === "REFUNDED") return "Remboursé";
  if (refundStatus === "CREDITED") return "Avoir crédité sur votre compte";
  if (status === "CANCELED" && refundStatus === "PENDING") return "Trajet annulé — remboursement en cours de traitement";
  if (status === "CANCELED") return "Trajet annulé";
  return null;
}
```

- [ ] **Step 5: Lancer + build**

```bash
cd backend && npx vitest run tests/cascade/passenger-serializer.test.ts && npm run lint
cd ../frontend/apps/passenger && npm run build 2>/dev/null || (cd ../../.. && cd frontend && npm run build)
```
Expected: test PASS, builds OK.

- [ ] **Step 6: Commit**

```bash
git add backend/src backend/tests/cascade/passenger-serializer.test.ts frontend
git commit -m "feat(cascade-01): expose refundStatus to passenger and render status labels"
```

---

### Task 9: Ouvrir NOTIFY-01 (email hors scope) + finalisation PR

> **Scope figé (ne pas ré-auditer, ne pas improviser) :** la vérification du provider email a
> été faite pendant le brainstorming — **aucun provider n'existe** (zéro dépendance email dans
> `backend/package.json` ; tous les matchs `EMAIL` du code sont des codes d'erreur métier, pas des
> envois). La branche « si un provider existe → envoi best-effort » **ne s'applique pas**. Cette tâche
> se limite donc **strictement à créer le ticket NOTIFY-01**. Aucune intégration, dépendance ou envoi
> email n'est ajouté dans CASCADE-01 (spec §9). Ne pas installer nodemailer/sendgrid/etc.

**Files:**
- Create: `docs/prd/active/NOTIFY-01-email-notifications.md` (stub de ticket)

- [ ] **Step 1: Créer le stub NOTIFY-01**

Documenter : besoin (notification email best-effort à l'annulation, §6/§9 du spec CASCADE-01), constat (aucun provider email dans le stack), scope à définir (choix provider, template annulation, envoi non bloquant). Marquer « à prioriser après CASCADE-01 ».

- [ ] **Step 2: Lancer toute la suite de tests**

```bash
cd backend && npm test
```
Expected: tous les tests cascade + sanity + schema PASS.

- [ ] **Step 3: Commit + push branche + ouvrir la PR**

```bash
git add docs/prd/active/NOTIFY-01-email-notifications.md
git commit -m "docs(cascade-01): open NOTIFY-01 for out-of-scope email notifications"
git push -u origin feature/cascade-01
gh pr create --base main --title "feat(cascade-01): trip cancellation cascade & refund policy" --body "Implémente CASCADE-01 (spec docs/superpowers/specs/2026-07-09-cascade-01-design.md). Review CTO requise avant merge."
```

---

## Notes transverses pour l'implémenteur

- **Seeds de test (vérifiés contre `schema.prisma`) :** `Line { name, startCity, endCity }`, `User { email (unique), firstName?, lastName?, userType }`, `Trip { lineId, departureTime }`, `Payment { userId, reservationId, stripePaymentIntentId?, amount (Decimal string), status, type }`. Le prix vit sur `Payment.amount`, jamais sur `Trip`/`Line`. Si un test échoue au seed, aligner sur le schéma — ne pas inventer de colonne.
- **`reservationInclude`** doit inclure `payment: true` (déjà le cas) et `refundProcessedBy: true` (à ajouter en Task 6) pour que la sérialisation refund fonctionne.
- **Ordre des tâches :** 1 → 2 sont des prérequis stricts. 3, 4, 5, 6 se suivent (4 et 5 consomment le helper de 3 ; 6 consomme les champs de 2). 7 dépend de 4/5/6. 8 est indépendant de 7. 9 clôt.
- **Aucun secret réel** dans `.env.test` versionné — seul `.env.test.example` est commité.
