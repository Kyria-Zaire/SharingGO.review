# Scripts — Sharing Go

| Script | Description |
|--------|-------------|
| `dev-up.ps1` | Lance `docker compose -f docker-compose.dev.yml up --build` |
| `dev-down.ps1` | Arrête la stack dev |

Scripts ops (backups, rabattage REC) : tickets infra ultérieurs.
